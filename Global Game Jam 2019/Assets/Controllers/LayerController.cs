using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LayerController : MonoBehaviour
{

    // private GameObject player;
    // private double interiorWarpX = 259;
    // private double interiorWarpY = 147;

    // private double exteriorWarpX = 0;
    // private double exteriorWarpY = 2;



    // this function will run in update to see if that player has hit a transfer spot, if so, it will change layers
    bool CheckForTransferSpot()
    {

        return true;
    } 

    // this function will switch layers based on if the player is inside or outside the house
    void TransferPlayer()
    {


    }

    // Start is called before the first frame update
    void Start()
    {
        // currentLayer = GameObject.Find("Tilemap");
        // player = GameObject.Find("Survivor");
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
